<UserControl x:Class="SisGUAPA.UI.WPF.Components.HeaderNew"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SisGUAPA.UI.WPF.Components"
             x:Name="Header"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <DockPanel VerticalAlignment="Top" Margin="5">
        <DockPanel.Effect>
            <DropShadowEffect/>
        </DockPanel.Effect>
        <materialDesign:Card
                          Height="40"
                          Padding="8"
                            UniformCornerRadius="0"
                          Background="{DynamicResource PrimaryHueDarkBrush}"
                          Foreground="{DynamicResource PrimaryHueDarkForegroundBrush}"
                          DockPanel.Dock="Top">
            <TextBlock Text="{Binding Title, ElementName=Header}" TextWrapping="Wrap" HorizontalAlignment="Left" VerticalAlignment="Center" FontWeight="Bold" FontSize="14" />
        </materialDesign:Card>
    </DockPanel>
</UserControl>
